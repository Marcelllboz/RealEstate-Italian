# Ventuno Giorni

21-day prep dashboard: Italian + South Australian real estate, before day one at the firm.

Static site — `index.html` is the whole app. Progress is saved in the browser (localStorage) with export/import for moving between devices.

Deployed on Vercel; pushes to `main` auto-deploy.
